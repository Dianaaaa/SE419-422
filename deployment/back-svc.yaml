apiVersion: v1
kind: Service
metadata:
  name: url-back
spec:
  type: NodePort
  ports:
  - port: 8000
    protocol: TCP
    targetPort: 8000
    name: http
    nodePort: 32200
  selector:
    app: back-pod